class Mermaid
attr_reader :name

    def initialize(name)
        @name = name
    end

    def initialize(name)
        @age = age
    end

    def initialize(get_older)
        @get_older = get_older
    end
end